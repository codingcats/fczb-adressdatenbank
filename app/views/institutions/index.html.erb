<div class="center jumbotron">

<h1>Institutionen</h1>
<p>&nbsp;</p>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th>Name &nbsp;</th>
      <th>Straße &nbsp;</th>
      <th>Plz &nbsp;</th>
      <th>Ort &nbsp;</th>
      <th colspan="4"></th>
    </tr>
  </thead>

  <tbody>
    <% @institutions.each do |institution| %>
      <tr>
        <td><%= institution.name %></td>
        <td><%= institution.street %></td>
        <td><%= institution.postcode %></td>
        <td><%= institution.city %></td>
        <td><%= link_to 'Show', institution %></td>
        <td><%= link_to 'Edit', edit_institution_path(institution) %></td>
        <td><%= link_to 'Destroy', institution, method: :delete, data: { confirm: 'Wirklich löschen?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to "Neue Institution anlegen".html_safe, new_institution_path, :id=>"open-contacts-dialog-btn", :class=>"btn btn-default btn-lg" %>
</div>
</div>